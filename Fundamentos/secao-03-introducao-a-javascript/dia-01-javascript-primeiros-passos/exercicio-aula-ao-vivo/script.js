/* 
   Agora vamos ter 15 minutos para construir um algoritmo com a seguinte descrição:
   Crie dois valores num1 e num2.
   Se os valores forem iguais some os dois
   Caso contrário multiplique num1 por num2.
   Por ultimo exiba o valor final no console.
*/

let num1 = 20;
let num2 = 30;
let resultado;

if (num1 === num2) {
  resultado = num1 + num2
}

else {
  resultado = num1 * num2
}

console.log(resultado)